# Backend environment variables (copy to .env for local overrides)
# Defaults below match docker-compose.yml and are safe for local development.

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=myapp

# HTTP server
PORT=3000
NODE_ENV=development
